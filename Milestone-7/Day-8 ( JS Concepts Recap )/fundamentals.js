/* 
    1. How to declare a variable using let and const
    2. Condition, 6 basic conditions >,<,===,!==,<=,>=
    2.1. Multiple conditions &&, ||
    3. Array declaration for example: const arr = [input,input,input]
    3.1. Index of array
    3.2. ArrayName.length
    3.3. Push and pop
    3.4 Set and change values with index number of an element inside an array
    4. Loop
    4.1. Basic For loop
    4.1. Other For loops
    5. Function
    5.1 Call Function
    6. Object (Collection of many elements and their values)
    6.1 Access property by name (3 ways) 
        way1: objectName.elementName;
        way2: objectName['elementName'];
        way3:const myVariable = 'elementName'; 
             objectName[myVariable]
    7. Browser API
    7.1. history api
    7.1. location api

    O T H E R S:
    => Array = map, forEach, Filter, Find
    => ternary operator
    => logical and &&
    => logical or ||
    => JSON.stringify/parse
    => string to number
    => number to string
*/